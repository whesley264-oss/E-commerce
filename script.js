document.addEventListener('DOMContentLoaded', () => { const cart = []; const products = [ { id: 1, name: 'Mini Pizza Margherita', price: 5.00, image: 'https://via.placeholder.com/150/FF0000/FFFFFF?text=Margherita' }, { id: 2, name: 'Mini Pizza Pepperoni', price: 6.00, image: 'https://via.placeholder.com/150/0000FF/FFFFFF?text=Pepperoni' }, { id: 3, name: 'Mini Pizza Frango com Catupiry', price: 7.00, image: 'https://via.placeholder.com/150/00FF00/FFFFFF?text=Frango' } ]; function displayProducts() { const productsContainer = document.getElementById('products-container'); productsContainer.innerHTML = ''; products.forEach(product => { const productCard = document.createElement('div'); productCard.className = 'product-card'; productCard.innerHTML = ` <img src='${product.image}' alt='${product.name}'> <h3>${product.name}</h3> <p>R$ ${product.price.toFixed(2)}</p> <button onclick='addToCart(${product.id})'>Adicionar ao Carrinho</button> `; productsContainer.appendChild(productCard); }); } function addToCart(productId) { const product = products.find(p => p.id === productId); if (product) { cart.push(product); updateCartDisplay(); } } function updateCartDisplay() { const cartItemsContainer = document.getElementById('cart-items'); cartItemsContainer.innerHTML = ''; let total = 0; cart.forEach((item, index) => { const cartItem = document.createElement('li'); cartItem.innerHTML = ` ${item.name} - R$ ${item.price.toFixed(2)} <button onclick='removeFromCart(${index})'>Remover</button> `; cartItemsContainer.appendChild(cartItem); total += item.price; }); document.getElementById('cart-total').textContent = total.toFixed(2); } function removeFromCart(index) { cart.splice(index, 1); updateCartDisplay(); } displayProducts(); });
